import type { VideoProject } from "@/lib/types/video";

// Empty by default so the dashboard demonstrates its empty state.
// Real projects will be fetched from the backend once storage exists.
export const mockProjects: VideoProject[] = [];
